{
  "name": "@drahoslavzan/nodeutils",
  "repository": "git://github.com/drahoslavzan/nodeutils.git",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/drahoslavzan"
  },
  "version": "1.0.1",
  "scripts": {
    "type-check": "tsc --noEmit",
    "build": "rm -rf ./dist && tsc && cp package.json .npmrc ./dist",
    "publish": "cd ./dist && npm publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^16.11.9",
    "typescript": "^4.5.2"
  }
}
